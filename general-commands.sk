command /help:
  permission: commands.help
  trigger:
    send "&7--- &8WorldHost &7---"
    send "&6&l/help&6> &eSends list of commands!"
command /joins:
  permission: commands.joins
  aliases: j, join
  trigger:
    send "&6&lUNIQUE JOINS> &e%{data::server::uj}%"
    send "&6&lTOTAL JOINS> &e%{data::server::tj}%"
